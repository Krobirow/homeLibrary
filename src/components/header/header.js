import React from 'react';
import './header.scss';

const Header = (props) => {
    return (
        <header className="header container-fluid">
            <h4 className={"mt-2 mb-2"}>
                Welcome on Krobirow's home library
            </h4>
        </header>
    );
}

export default Header;
