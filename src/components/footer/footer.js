import React from 'react';
import './footer.scss';

const Footer = (props) => {
    return (
        <footer className="footer container-fluid">
            <p className={"m-1"}>Krobirow dev. Copyright © all right reserved.</p>
        </footer>
    );
}

export default Footer;
